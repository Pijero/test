# Assemblies

`BattleMoodAura.dll` をこのフォルダに配置してください。

## ビルド方法
1. `Source/BattleMoodAura/BattleMoodAura.csproj` を Visual Studio で開く
2. 環境変数 `RimWorldWin64Dir` を設定（RimWorldのインストールディレクトリ）
3. Build を実行

このリポジトリの csproj は `..\..\Assemblies\` へ出力される設定です。
